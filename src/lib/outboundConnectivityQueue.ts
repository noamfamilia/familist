/** Shown on sync_queue rows while waiting after a connectivity-classified failure. */
export const OUTBOUND_CONNECTIVITY_QUEUE_DETAIL =
  'Waiting for internet connection — will retry when the network is back.'
